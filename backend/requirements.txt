# ============================================================
# NexControl Backend - Python Dependencies
# ============================================================

# Web Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0

# System Monitoring & Process Management
psutil>=5.9.6
pyautogui>=0.9.54
pywin32>=306  # Windows-specific window control for targeted media commands

# Docker Integration
docker>=6.1.3

# Security & Encryption
cryptography>=41.0.7
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
argon2-cffi>=23.1.0

# Rate Limiting
slowapi>=0.1.9
limits>=3.6.0

# Utilities
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
pyperclip>=1.8.2

# Screenshot Support (Pillow is required by pyautogui)
Pillow>=10.1.0

# Optional: GPU Temperature (NVIDIA)
# nvidia-ml-py is NVIDIA's official library - supports RTX 40-series
nvidia-ml-py>=12.535.133
# GPUtil as fallback for older systems
gputil>=1.4.0

# GUI Support
customtkinter>=5.2.2
packaging>=23.2
pystray>=0.19.5
qrcode[pil]>=7.4.2  # QR code generation for mobile app setup

# Development (Optional - remove in production)
pyinstaller>=6.3.0
